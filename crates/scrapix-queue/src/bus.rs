//! Concrete enum wrappers for the message bus abstraction.
//!
//! This module is kept for backwards compatibility. The actual types now live in
//! [`crate::traits`] where they are co-located with the trait definitions.
//! Everything is re-exported from the crate root via `pub use traits::{AnyConsumer, AnyProducer}`.
